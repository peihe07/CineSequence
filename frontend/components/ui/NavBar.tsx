'use client'

// NavBar functionality has been merged into Header (unified top navigation).
// This component is kept as a no-op so existing imports do not break.
// Safe to remove this file and the NavBar import in layout.tsx in a future cleanup.
export default function NavBar() {
  return null
}
