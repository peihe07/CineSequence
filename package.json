{
  "name": "movie-dna",
  "private": true,
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && alembic upgrade head && uvicorn app.main:app --reload",
    "typecheck:frontend": "cd frontend && npx tsc --noEmit",
    "test:frontend": "cd frontend && npx vitest run stores/authStore.test.ts 'app/(auth)/register/page.test.tsx' 'app/(auth)/verify/page.test.tsx'",
    "test:backend:security": "cd backend && pytest tests/test_main.py tests/test_auth.py tests/test_profile.py tests/test_admin.py tests/test_sequencing_router.py",
    "smoke:backend": "sh scripts/backend-smoke.sh",
    "docker:dev": "docker compose up --build",
    "docker:prod": "docker compose --env-file .env.production -f docker-compose.prod.yml up --build -d"
  },
  "version": "1.0.0",
  "description": "A movie-taste-based social matching platform. Users go through a 20-round binary movie selection process (\"sequencing\") that builds a multi-dimensional taste profile (\"Movie DNA\"), then get matched with people who share similar cinematic preferences.",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
